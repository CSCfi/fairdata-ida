--- /var/ida/nextcloud-old/core/js/share.js	2021-10-27 10:25:31.029496617 +0000
+++ /var/ida/nextcloud-old/core/js/share.js.original	2021-10-27 10:25:31.029496617 +0000
@@ -210,8 +210,7 @@
 		var parts = this._REMOTE_OWNER_REGEXP.exec(shareWith);
 		if (!parts) {
 			// display avatar of the user
-			//var avatar = '<span class="avatar" data-username="' + escapeHTML(shareWith) + '" title="' + message + " " + escapeHTML(shareWithDisplayName) + '"></span>';
-			var avatar = '<span class="avatar" data-username="' + escapeHTML(shareWith) + '" aria-label="Open sharing tab"></span>';
+			var avatar = '<span class="avatar" data-username="' + escapeHTML(shareWith) + '" title="' + message + " " + escapeHTML(shareWithDisplayName) + '"></span>';
 			var hidden = '<span class="hidden-visually">' + message + ' ' + escapeHTML(shareWithDisplayName) + '</span> ';
 			return avatar + hidden;
 		}
